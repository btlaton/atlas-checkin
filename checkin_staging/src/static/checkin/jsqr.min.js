/* Local jsQR placeholder. Replace this file with the real jsQR library (https://github.com/cozmo/jsQR) for full offline fallback.
This stub prevents network calls but does not perform decoding. Native BarcodeDetector remains primary. */
window.jsQR = window.jsQR || function(){ return null; };
